class PaymentManager{
    constructor(title){
        this.title = title

    }

    render(target){
        let table = $('<table>')
        let caption = $('<caption>').text(`${this.title} Payment Manager`)
        let thead = $('<thead>')

    }
}